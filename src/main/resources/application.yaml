spring:
   datasource:
     username: dnd_party
     password: dnd_party
     url: jdbc:mysql://localhost:3306/dnd_party
     
   jpa:
     hibernation:
       ddl-auto: none
     show-sql: true
   
   sql:
      init:
         mode: always

      
